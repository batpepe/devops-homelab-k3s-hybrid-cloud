apiVersion: apps/v1
kind: Deployment
metadata:
  name: batman-app
  namespace: apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: batman
  template:
    metadata:
      labels:
        app: batman
    spec:
      containers:
      - name: batman
        image: ghcr.io/batpepe/batman-app:480778f958c25e6bfba5d19c1ca731b86e4bb008
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: batman-svc
  namespace: apps
spec:
  selector:
    app: batman
  ports:
  - port: 80
    targetPort: 80
---
# Public host routed by the Cloudflare Tunnel wildcard through Traefik.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: batman-ingress
  namespace: apps
spec:
  rules:
  - host: game.batpepe.online
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: batman-svc
            port:
              number: 80
